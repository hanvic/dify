# REQUALITY_REPORT.md

> 작성일: 2026-08-02 15:20 KST

## 1. 검색 품질 전후 비교

### 테스트 조건

- **데이터셋**: `20087ab8-8e76-4f75-bfc8-88a24f4fd73c`
- **검색 방식**: semantic_search, top_k=10, score_threshold 미적용
- **이전 설정**: delimiter=`\n\n`, max_chunk_length=500, overlap=100 → 페이지당 평균 13조각
- **새 설정**: delimiter=`\n\n---\n\n`, max_chunk_length=6000, overlap=0 → 페이지당 1조각

### 이전 설정 검색 결과 (삭제 전)

| 질의                     | 상위5 평균 길이 | 30자 미만 비율 | 표 포함 | 상위1 내용                             |
| ------------------------ | --------------- | -------------- | ------- | -------------------------------------- |
| 원천징수 영수증 소득세   | 17자            | 60%            | 없음    | `원천징수영수증 (소득자보관용)` (16자) |
| 금전출납부 복지자금 잔액 | 7자             | **100%**       | 없음    | `원리금 지급 사항` (9자)               |
| 급여 계좌이체 내역       | 5자             | **100%**       | 없음    | `수당명세서` (5자)                     |
| 구호기금 세입세출 결산   | 11자            | **100%**       | 없음    | `구호기금 세입세출 결산서` (13자)      |
| 갑근세 원천세 납부       | 10자            | **100%**       | 없음    | `갑근세 기타서류` (8자)                |
| 예금이자 은행 거래내역   | 8자             | **100%**       | 없음    | `정기예금 해지내역` (9자)              |

**문제점**: 6개 질의 중 5개에서 상위 결과의 100%가 30자 미만의 무의미한 제목 조각. 표 구조는 완전히 소실.

### 새 설정 문서 품질 (세그먼트 실물)

**문서 p24** (doc_id: `97625a3c`, 세그먼트 1개, 1,499자):

```
기호설명
ARM : 육군급여
ATM : ATM인, 출금
...
국민은행

| 년월일 | 적요 | 지급금액 | 예입금액 | 차감잔액 | 비고 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 94-01-25 | | | | 첫거래감사합니다 "VV" | |
| 94.01.25 | | | 9,600,000 | *9,600,000 | 09 |
| 94-01-25 | | 750,000 | | *8,850,000 | 09 |
...
```

→ **마크다운 표가 완벽하게 보존됨**. 기호 설명과 거래내역이 하나의 맥락으로 검색 가능.

**문서 p26** (doc_id: `ba5b469a`, 세그먼트 1개, 67자):

```
1993년도
구호기금 세입세출 결산서
1993년 1월 1일
1993년 12월 31일
전 국 재 해 대 책 협 의 회
```

→ 표지 페이지로, 연도/기관명이 온전하게 보존됨.

### 이전 설정의 동일 페이지(p23) 과분할 실태

문서 p23 (1,249자 → **13조각**):

```
pos=1 | 23자 | (헤더) [별지 제 101호 서식 (1)]
pos=2 | 26자 | □ 원천징수영수증 □ 지급조서 (소득자 보관용)
pos=3 |  4자 | 관리번호
pos=9 |  5자 | 년 월 일
pos=10|  2자 | 귀하
```

→ 표 3개 조각 분리, "관리번호"(4자), "귀하"(2자) 같은 무의미한 세그먼트 생성

### 결론

새 설정은 **표 온전성 100% 보존**, **맥락적 검색 정확도 향상**, **쓰레기 세그먼트 제거**를 달성. 이전 설정 대비 압도적 품질 개선.

---

## 2. 재처리 판단과 근거

### 판단: **재처리 합당** ✓

| 항목                      | 수치                                    |
| ------------------------- | --------------------------------------- |
| 과분할된 문서 (삭제 대상) | 926건 (error/waiting 6건 포함)          |
| 새 설정 문서 (보존)       | 2건                                     |
| 재처리 대상 페이지        | 893페이지                               |
| 전체 대비                 | 893 / 27,754 = **3.2%**                 |
| 예상 VLM 호출             | ~893회 (페이지당 1회)                   |
| 예상 소요 시간            | ~1.5시간 (현재 배치 속도 기준 ~10건/분) |

### 근거

1. **검색 품질**: 이전 설정에서 100%의 질의가 무의미한 단편을 반환. 새 설정은 표/맥락을 온전히 보존.
2. **비용**: 893 VLM 호출은 전체 27,754의 3.2%로 소규모. Ollama Cloud 무료 티어로 커버 가능(시간 분산 시).
3. **위험**: API 삭제로 벡터까지 정리되므로 데이터 정합성 유지. 백업 CSV 존재.

---

## 3. 삭제 전후 수치

| 측정항목                | 삭제 전 | 삭제 후                                     |
| ----------------------- | ------- | ------------------------------------------- |
| 문서 수                 | 928     | **2**                                       |
| 세그먼트 수 (추정)      | ~7,300  | **2**                                       |
| error 문서              | 5       | 0                                           |
| waiting 문서            | 1       | 0                                           |
| 30자 미만 세그먼트 비율 | ~39%    | **0%**                                      |
| 삭제 방법               | -       | Dify API (`DELETE /documents/{id}`)         |
| 삭제 소요 시간          | -       | 65초                                        |
| 실패 건                 | -       | 0                                           |
| 백업                    | -       | `scripts/oversplit_docs_backup.csv` (926행) |

---

## 4. 상태파일 재조정 수치

| 파일                    | 변경 전 | 변경 후                                        |
| ----------------------- | ------- | ---------------------------------------------- |
| `processed_pages.jsonl` | 895행   | **2행**                                        |
| 제거된 key              | -       | 893행                                          |
| 백업                    | -       | `processed_pages.jsonl.bak.requality_20260802` |

제거된 893 key = 재처리 대상으로 복귀. 배치 러너가 다음 실행 시 자동으로 감지하여 재처리합니다.

---

## 5. 메타데이터 재주입 확인

| 항목                               | 결과                                            |
| ---------------------------------- | ----------------------------------------------- |
| `inject_metadata.py --dry-run`     | 정상 (2건 인식, 5개 필드)                       |
| `inject_metadata.py --force`       | 2건 매핑 성공, 주입 완료                        |
| `inject_metadata.py --verify-only` | 메타있음 2, 없음 0                              |
| 재처리 후 동작                     | 새 문서 생성 시 `--force` 없이도 자동 주입 가능 |

`inject_metadata.py`는 문서명 패턴에서 base_key를 추출하여 index.db3와 매핑하므로, doc_id 변경에 무관하게 정상 동작합니다.

---

## 6. 오류 상태 문서 정리

6건 (error 5 + waiting 1) 모두 삭제 완료:

- `cc253dbd` - 금전출납부 2002 구호기금 1_p1 (error)
- `d5a5698a` - 금전출납부 2002 구호기금 1_p0 (error)
- `7067d4bc` - 금전출납부 2000 복지자금\_p25 (error)
- `f76db86d` - 금전출납부 2000 복지자금\_p24 (error)
- `2279cffa` - 금전출납부 2000 복지자금\_p23 (error)
- `3fc1c46e` - 금전출납부 1 복지자금\_p23 (waiting)

해당 key가 processed_pages.jsonl에서도 제거되어 재처리 대상으로 복귀.

---

## 7. 남은 작업

1. **배치 재처리 실행** (다른 에이전트 담당): 893페이지를 새 설정으로 재처리
2. **메타데이터 재주입**: 재처리 완료 후 `python3 scripts/inject_metadata.py` 실행
3. **벡터 인덱스 확인**: 대량 삭제 후 일시적으로 retrieve 결과가 0건일 수 있음. 새 문서 적재 후 정상화 예상.
4. **검색 품질 최종 검증**: 재처리 완료 후 동일 질의 6개로 재검증 권장

---

## 산출물 목록

- `scripts/oversplit_docs_backup.csv` — 삭제된 926건 백업
- `scripts/delete_oversplit_docs.py` — 삭제 스크립트
- `scripts/oversplit_cleanup_progress.txt` — 삭제 진행 로그
- `batch_state/processed_pages.jsonl.bak.requality_20260802` — 상태파일 원본 백업
- `planning/REQUALITY_REPORT.md` — 본 보고서
